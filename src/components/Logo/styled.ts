import { styled } from 'styled-components';

export const Wrapper = styled.div`
    width: 2em;
    aspect-ratio: 1 / 1;
`;

export const Image = styled.img`
    width: 100%;
    height: 100%;
    aspect-ratio: 1 / 1;
`;
