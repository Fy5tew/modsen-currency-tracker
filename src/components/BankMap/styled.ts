import { styled } from 'styled-components';

export const MapWrapper = styled.div`
    height: 600px;
    border-radius: 1em;
    overflow: hidden;

    & > * {
        width: 100%;
        height: 100%;
    }
`;
